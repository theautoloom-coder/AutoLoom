node_modules/
.expo/
dist/
web-build/
*.log
.env
.env.*.local
app/public/@powersync/
supabase/.temp/
supabase/.branches/
.DS_Store
Thumbs.db

# Screenshots written by the test scripts on every run. Regenerated by
# test/live-check.mjs, flow-walk.mjs and e2e-staff.mjs; nothing reads them back.
app/test/screens/

# A cloned MCP tool that happens to live in this folder. It carries its own
# .git and its own node_modules, and it is not part of the product.
uiverse-mcp/

# Build artefacts fetched for device testing. The APK is 50 MB and is rebuilt
# by EAS on demand; dist-local/ is a throwaway web build pointed at the local
# Supabase so write tests never touch the real shop's data.
*.apk
app/dist-local/
