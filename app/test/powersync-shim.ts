/** In tests, `@powersync/react-native` resolves here: the pure-JS schema classes only. */
export { column, Schema, Table } from '@powersync/common';
export type { AbstractPowerSyncDatabase, Transaction, CrudEntry, PowerSyncBackendConnector } from '@powersync/common';
export { UpdateType } from '@powersync/common';
